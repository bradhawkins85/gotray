# Sample configuration for GoTray

# Optional: override the configuration file path. Useful for running side-by-side environments.
# GOTRAY_CONFIG_PATH=/var/lib/gotray/<user>/config.b64

# Optional: IPC token for the background service, when used.
# GOTRAY_SERVICE_TOKEN=generate-a-strong-token

# When launching without explicit arguments the binary defaults to the "run" mode.
# Override this to force another command when embedding the binary elsewhere.
# GOTRAY_RUN_MODE=run

# Tactical RMM integration. All values are optional but required for remote menu overrides.
# TRMM_BASE_URL=https://api.example.com/api/v3
# TRMM_AGENT_ID=AGENT_ID_HERE
# TRMM_AGENT_PK=0
# TRMM_SITE_ID=0
# TRMM_CLIENT_ID=0
# TRMM_APIKEY=your-api-key
# Legacy casing TRMM_APIKey is still supported for compatibility, but TRMM_APIKEY
# matches the repository secret configured in CI.
#
# Testing only: bypass the compile-time Tactical RMM API key requirement.
# GOTRAY_ALLOW_RUNTIME_TRMM_APIKEY=1

# Build automation toggles used by the release scripts. Compression remains
# disabled by default and is skipped automatically on macOS to avoid codesign
# issues. Windows builds disable symbol stripping automatically to reduce
# false-positive antivirus detections. Override GOTRAY_STRIP_SYMBOLS to force
# explicit behaviour.
# GOTRAY_ENABLE_COMPRESSION=0
# GOTRAY_COMPRESSION_TOOL=upx
# GOTRAY_COMPRESSION_ARGS=--best --lzma
# GOTRAY_SKIP_COMPRESSION_OS=darwin
# GOTRAY_OUTPUT_DIR=dist
# GOTRAY_STRIP_SYMBOLS=1
# GOTRAY_EXTRA_LDFLAGS=
